function checkObj(obj, checkProp) {
  // Cambia solo el código debajo de esta línea

if (obj.hasOwnProperty(checkPro)) {;
    return obj.checkProp;
  } else {

    return "Not Found";
  }
  // Cambia solo el código encima de esta línea
}

const cosas = {
  gift: "pony",
  pet: "kitten",
  bed: "sleigh"
};

res1 = checkObj(cosas,"gift");
console.log(res1);
